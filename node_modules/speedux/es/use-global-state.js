/**
 * Dependency imports
 */
import { useSelector } from 'react-redux';
import { queryObject } from 'noyb';
export default (function (query) {
  return useSelector(function (globalState) {
    return query ? queryObject(query, globalState) : globalState;
  });
});